<Page x:Class="PI.Schedule"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PI"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="700"
      Title="Schedule">

    <Grid Margin="-5,-5,-5,0">
        <Grid.Background>
            <LinearGradientBrush>
                <GradientStop Color="#DCDCDC"/>
            </LinearGradientBrush>
        </Grid.Background>
        <StackPanel>
            <StackPanel Orientation="Horizontal" Height="75" Margin="0,30,0,0">
                <DatePicker Margin="115,20,150,20" FontSize="12" Width="158" Name="FindDatePicker" Height="24"/>
                <Button Width="170" Margin="0,10" Height="30" FontSize="20" Foreground="Black" Name="FindButton" Content="Find" Click="FindButton_Click"/>
            </StackPanel>
            <DataGrid Height="350" Margin="25,15,25,0" Name="FindDataGrid"/>
        </StackPanel>
    </Grid>
</Page>
